#include <iostream>
#include <atomic>
#include <thread> 
#include <ipfs/client.h>
// Or when you installed IPFS locally, use:
//#include "ipfs/client.h"

/**
 * Assuming you build & installed IPFS client already.
 * You can compile this example code yourself:
 *
 * c++ -std=c++17 -lipfs-http-client threading_example.cc -o threading_app
 */

int main() {
    // TODO
    return 0;
}